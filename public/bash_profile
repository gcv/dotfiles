# .bash_profile is only executed for interactive shells

shopt -s autocd

PS1='[\u@\h:\W]\$ '

# additional non-Bash sh-type shell configuration:
# commented out because it loads automatically in most cases?
#[[ -f ~/.profile ]] && . ~/.profile
